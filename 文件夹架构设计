/data/data/xzy/CulturalVideoAnnotation/
├── data/                    # 核心数据仓
│   ├── raw/                 # 北京台提供的 8K/4K 原始素材（只读，不要修改）
│   ├── processed/           # 预处理后的数据（切片后的片段、关键帧）
│   │   ├── clips/           # 5-10秒的小片段
│   │   └── frames/          # 提取的关键帧张量
│   └── annotations/         # 标注文件（JSON/Excel格式）
│       ├── manual/          # 你的 50 个人工标注真值 (Ground Truth)
│       └── auto/            # 模型自动生成的预标注结果
├── models/                  # 模型管理
│   ├── pretrained/          # 原始下载的权重（如 Qwen-VL-Chat）
│   └── checkpoints/         # 你后续微调或优化后的模型权重
├── src/
│   ├── __init__.py              # 包初始化文件
│   ├── config.py                # 配置管理模块
│   ├── model_manager.py         # 模型管理模块
│   ├── prompt_manager.py        # 提示词管理模块
│   ├── video_processor.py       # 视频处理模块（包含ffmpeg元数据获取）
│   ├── inference_engine.py      # 推理引擎模块
│   ├── result_processor.py      # 结果处理模块
│   └── run_cultural_annotation.py  # 主程序（重构后）
├── outputs/                   # 输出目录
├── experiments/             # 实验记录与结果
│   ├── logs/                # 训练和推理日志
│   └── results/             # 对比实验的图表、指标分析
└── docs/                    # 文档管理
    ├── ontology/            # 文化特征本体定义文档
    └── thesis/              # 论文草稿、开题报告、参考文献
    


🔧 各模块功能说明

1. **config.py** - 配置管理模块
- 集中管理所有配置参数
- 包含GPU、模型、视频、生成、路径等配置类
- 支持动态配置更新
- 提供便捷的参数获取方法

2. **model_manager.py** - 模型管理模块
- 负责加载和管理Qwen3.5模型
- 支持模型的加载、卸载和内存管理
- 提供模型和处理器访问接口

3. **prompt_manager.py** - 提示词管理模块
- 管理EMCAST六维分层标注提示词
- 支持视频元数据动态集成到提示词
- 提供自定义提示词创建功能

4. **video_processor.py** - 视频处理模块 ⭐
- **使用ffmpeg-python获取视频元数据**（时长、帧率、分辨率、编码格式等）
- 视频文件验证
- 视频信息格式化输出
- 支持视频输入准备

5. **inference_engine.py** - 推理引擎模块
- 整合所有模块进行推理
- 支持单个视频和批量视频处理
- 自动处理视频元数据并集成到提示词

6. **result_processor.py** - 结果处理模块
- 标注结果的结构化管理
- 支持多种输出格式（txt、json）
- 提供结果格式化和保存功能
- 支持层级内容提取和结果比较

7. **run_cultural_annotation.py** - 主程序（重构后）
- 清晰的命令行参数解析
- 支持单个视频和批量处理
- 完善的错误处理和资源清理
- 详细的进度显示

🚀 使用方法

单个视频处理
```bash
python /data/data/xzy/CulturalVideoAnnotation/src/run_cultural_annotation.py \
  --video /data/data/xzy/CulturalVideoAnnotation/data/2.mp4 \
  --verbose \
  --output-format both
```

批量视频处理
```bash
python /data/data/xzy/CulturalVideoAnnotation/src/run_cultural_annotation.py \
  --video-dir /data/data/xzy/CulturalVideoAnnotation/data \
  --output-format txt
```

自定义参数
```bash
python /data/data/xzy/CulturalVideoAnnotation/src/run_cultural_annotation.py \
  --video /path/to/video.mp4 \
  --gpu 5 \
  --fps 0.5 \
  --max-frames 100 \
  --output-dir /custom/output/path \
  --output-format json \
  --verbose
```

🎯 主要改进

1. **模块化设计**：每个模块职责单一，易于维护和扩展
2. **ffmpeg集成**：使用ffmpeg-python获取视频元数据，自动集成到提示词中
3. **配置管理**：集中配置管理，便于参数调整
4. **批量处理**：支持批量视频处理，提高效率
5. **多种输出格式**：支持txt、json和both格式
6. **错误处理**：完善的错误处理和资源清理机制
7. **命令行接口**：清晰的命令行参数，使用便捷

✅ 测试结果

已成功测试：
- ✅ 单个视频处理
- ✅ 批量视频处理（2个视频）
- ✅ 结果保存（txt和json格式）
- ✅ ffmpeg元数据获取
- ✅ GPU设备管理

所有功能正常工作，代码结构清晰，易于维护和扩展！